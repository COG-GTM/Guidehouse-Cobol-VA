In COG-GTM/Guidehouse-Cobol, extract the embedded SQL behavior into demo-ready Oracle SQL notes.

Scope:
- Extract LABD20 duplicate check, insert, count update, commit, and stats queries.
- Extract CONTROL-RECORD-TABLE-IO CRUD operations.
- Convert any dynamically constructed SQL into parameterized SQL examples.
- Include source file and line citations.
- Place output in converted-code/sql/.
- Do not modify original source files under source/.
